
project.ext {
    dropwizardVersion = '0.7.1'
}

subprojects {
    version = '0.1.0'
    apply plugin: 'java'
    apply plugin: 'eclipse-wtp'
    repositories {
        mavenCentral()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.0'
}
